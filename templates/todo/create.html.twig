{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(todo_form) }}
    {{ form_widget(todo_form) }}

    <div class="form-group row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10 text-right">
            <input type="submit" value="Save" class="btn btn-success">
        </div>
    </div>

    {{ form_end(todo_form) }}
{% endblock %}